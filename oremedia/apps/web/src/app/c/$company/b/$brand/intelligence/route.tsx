import { IntelligenceWorkspaceScreen } from '../../../../../../features/intelligence/intelligence-workspace';

/** `intelligence`: the five views of spec 16.9; the feature slice owns the screen. */
export function IntelligenceRoute() {
  return <IntelligenceWorkspaceScreen />;
}
