import { ChannelSettingsScreen } from '../../../../../../features/publishing/channel-settings';

/** `settings`: channel connections (spec 14.7); the feature slice owns the screen. */
export function SettingsRoute() {
  return <ChannelSettingsScreen />;
}
