import { CampaignsScreen } from '../../../../../../features/content/campaigns-screen';

/** `campaigns`: planner from brief to plan to assigned work (spec 21.1); the feature slice owns the screen. */
export function CampaignsRoute() {
  return <CampaignsScreen />;
}
