import { ExperimentsScreen } from '../../../../../../features/experiments/experiments-screen';

/** `experiments`: design, pre-registration, lifecycle and results (spec 16.6); the feature slice owns the screen. */
export function ExperimentsRoute() {
  return <ExperimentsScreen />;
}
