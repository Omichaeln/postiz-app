import { AgentRunsScreen } from '../../../../../../features/agents/agent-runs';

/** `agents`: runs, steps, costs and exceptions (spec 21.1); the feature slice owns the screen. */
export function AgentRunsRoute() {
  return <AgentRunsScreen />;
}
