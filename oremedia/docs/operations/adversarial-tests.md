# Adversarial test map (spec 19.2, ledger T.10)

Spec 19.2: every critical-tier path in section 18 has at least one adversarial test. One row per section 18
boundary; each cell names the test file and the test (`it`) that attacks it. "None yet" is stated where true.

| #    | Boundary / threat (spec 18)                          | Adversarial tests (file → test name)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ---- | ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| S.1  | Caller → another tenant's object (IDOR/BOLA)         | `tooling/test-fixtures/cross-tenant.cross-tenant.test.ts` → "$path rejects foreign tenant resources", "REST $method $path rejects foreign tenant resources", the MCP `it.each` cases, "a creator restricted to brand 1 cannot see brand 2 of the same tenant", "an API client of tenant B cannot select tenant A". Exports: `apps/api/src/export-disclosure.integration.test.ts` (row S.8). Workers: `packages/activities/src/render-job.integration.test.ts` → "cross-tenant: tenant B cannot pin tenant A’s assets, read its objects or store under its prefix".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| S.2  | Retrieved content → agent instructions (injection)   | `packages/modules/agents/src/injection.integration.test.ts` → "%s: obeyed tool calls cannot widen scope, reach unlisted tools, raise the mode or publish" (fixtures in `tooling/test-fixtures/injection`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| S.3  | Agent → publication authority                        | `packages/domain/src/policy.test.ts` → "7. agents: never actions, grant ∩ autonomy, channel scope", "7b. an agent cannot raise its own mode…"; `packages/ai/src/tool-dispatcher.test.ts` → "refuses a tool with effect external: no agent tool publishes"; `packages/modules/review/src/review.integration.test.ts` → "a revoked approver blocks dispatch (Phase 5 gate)…", "expired approvals, timing outside the binding, a dead channel…each hold", "with the flag on for the tenant and the entitlement, an owner creates a mandate…" (mandate_channel, mandate_daily_quota).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| S.4  | Skill package → runtime                              | `packages/modules/skills/src/package-format.test.ts` → "malicious package fixtures (spec 18…) > %s is refused with its typed reasons", "…covers every attack class the threat model names"; `packages/modules/skills/src/skills.integration.test.ts` → "every malicious package fixture is refused at import with its typed reasons, and nothing is written (spec 18)" (fixtures in `tooling/test-fixtures/skill-packages`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| S.5  | Upload/render → host or network                      | Ingest: `packages/modules/assets/src/ingest/steps.test.ts` → "rejects an SVG carrying %s", "rejects entity declarations before any parsing", "rejects an SVG that exceeds the pixel budget…", "rejects a decompression bomb from its header, before decoding", "rejects a truncated or garbage font", "the EICAR string is detected…"; `assets.integration.test.ts` → "…an SVG with a script is rejected and cleaned up". Render: `packages/activities/src/render-job.integration.test.ts` → "an asset version whose bytes were replaced after pinning fails the render with RenderIntegrityError and writes no export (spec 18)"; `apps/worker-render/src/render-isolation.integration.test.ts` → "a format larger than the render limit is refused…", "a page with more than 300 elements is refused…", "nested groups are counted…", "a page script cannot reach the network: fetch, XHR, image, WebSocket, beacon, iframe and navigation all fail". SSRF/redirect chains on ingest: not applicable in Release 1 (no URL import; uploads are presigned PUTs); provider-side SSRF is row S.13. |
| S.6  | Credential store → model/log/history                 | `apps/worker-core/src/secret-scan.integration.test.ts` → "the secrets appear nowhere: model requests, outbox, audit, logs, workflow history, agent rows, evidence, credential rows" (with the publication, token-refresh and agent-run scenarios before it); `packages/observability/src/allowlist.test.ts` → "the logger never emits a token even under an allowed key"; `packages/modules/publishing/src/envelope.test.ts` → "round-trips with the right AAD and produces no plaintext in the envelope".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| S.7  | Retry/cancel → duplicate publication                 | `apps/worker-core/src/publishing.integration.test.ts` → "crash after send → outcome_unknown → reconciled…", "a duplicate outbox delivery starts the same workflow id once…", "cancel race: …"; `packages/modules/publishing/src/publishing.integration.test.ts` → "a retryable error proven pre-send (no sentAt) goes back to scheduled…; with sentAt it is unknown", "reschedule updates the row…; it never terminates it"; `packages/providers/src/io.test.ts` → "a timeout after the request was sent is after_send (never retried by the caller)".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| S.8  | Portfolio/export → client data disclosure            | `apps/api/src/export-disclosure.integration.test.ts` → "as a member of both clients, every query answers for the selected client only, never with the other client’s rows", "a revoked or an expired external reviewer link reads nothing through any query"; `review.integration.test.ts` → "records the verified email…and the link is single-use" (post-revocation use).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| S.9  | Expensive generation → runaway cost                  | `packages/modules/billing/src/budgets.integration.test.ts` → "parallel reservations cannot overspend the brand-day limit"; `agents.integration.test.ts` → "N concurrent reservations against one brand-day limit…"; `packages/ai/src/provider-jobs.test.ts` → "the provider accepted, then the poll timed out: the retried activity polls the same job; one submission", "a tool timeout while the provider is still processing is denied tool_timeout; the same step then polls, not resubmits"; `packages/workflows/src/publication.workflow.v1.test.ts` → "unknown (the activity threw: timeout, worker loss) → markOutcomeUnknown…".                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| S.10 | External reviewer link → escalation                  | `review.integration.test.ts` → "sees only the frozen manifest of their request; other requests and the inbox are out of scope", "records the verified email…single-use", "expired links are refused…"; `apps/api/src/export-disclosure.integration.test.ts` → "a revoked or an expired external reviewer link reads nothing through any query".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| S.11 | Platform operator → tenant data                      | `apps/api/src/support-sessions.integration.test.ts` → "a read-only session reads, cannot write, cannot escalate itself; a second operator escalates; every request is audited with its supportSessionId", "an expired session reads nothing and the refused request is still audited", "a session is bound to its tenant…"; `packages/domain/src/policy.test.ts` → "platform operator: read-only unless escalated; never decides or publishes".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| S.12 | Provider webhooks inbound                            | None yet — not applicable in Release 1: no inbound webhook endpoint exists (`apps/api` serves `/health`, `/trpc`, `/v1`, `/mcp`; the redirector serves `/:code`); ingestion is scheduled pulls (spec 4.4) and CRM webhooks are Release 2 (spec 15). A forged/replayed test is required when the first endpoint lands.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| S.13 | Outbound webhooks (tenant-configured) / provider I/O | `packages/providers/src/ssrf.test.ts` → "blocks private, loopback, link-local…", "IPv6 in any notation…", "URL policy checks IPv6 literals…", "a lookup answer with any blocked address is refused as a whole…", "the pinned dispatcher resolves once and connects to the checked (first) address, never the rebound one", "control: validate-then-connect with an unpinned agent is rebound to the internal target", "a later connection that resolves to the internal address is refused by the same pinned check". Tenant-configured outbound webhooks do not exist in Release 1; the dispatcher under test is the one ProviderIO uses.                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
